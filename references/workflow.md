# 工作流（Workflow，5 步）

> **打包说明**：本文件由 `03_工作流_workflow/01–05` 合并转换生成（仅编号与路径口径不同），**禁止直接修改，修改请改源**；维护性章节不打包，完整版见源文件。
>


## 1. 第 1 步 分析市场（Analyze Market）——工作流入口

> **本文件定位**：整个分析流程的第一步，只回答"怎么读取数据、怎么确定级别、怎么确定趋势方向"。数浪方法见 `references/workflow.md（第 2 步）`，本文件只建立衔接，不重复数浪。
>
> **前置依赖**：术语见 `references/terminology.md`（references/terminology.md 1.2.1–1.2.3、1.7.7），规则见 `references/rules.md`（references/rules.md R4、R20、R22），概率经验见 `references/guidelines.md`（references/guidelines.md G3）。

### 1. 数据读取与准备

#### 1.1 数据来源

- 分析 A 股时可引用 `a-stock-data` skill 获取行情数据（K 线、极端价位、资金面等真实数据）。
- A 股取数的确定性协议（需求→函数映射、参数默认值、CSV 输出契约、降级链）见 `references/data.md`（唯一权威）。
- 其他市场（港股、美股、期货、加密货币等）由用户提供数据或图表（引用 AGENTS.md 数据与打包章节）。

#### 1.2 数据要求

- 至少提供与声明的分析级别匹配的历史 K 线数据，包含最高价、最低价、收盘价、开盘价。
- 周期与级别匹配：常见时间尺度可参考九级对照表（references/terminology.md 1.2.2），例如日线级别对应日 K、中浪级别对应日/周 K、细浪级别对应小时 K。
- 复权口径：长周期/大级别默认目标后复权（hfq），短中期可前复权（qfq）；mootdx bars() 为不复权原始价，无除权除息数据时不得自行计算 hfq，自动降级腾讯 fqkline 前复权并披露 qfq，禁止声称 hfq；同一分析内禁止混用（引用 `references/data.md` 第 2 节）。

#### 1.3 图表准备

- 若用户提供截图/图表，先提取关键价位：**极端价位（最高价/最低价，含影线与盘中极端点）**；所有后续计算以此为准（references/rules.md R20）。
- 截图无法确认影线细节时，需向用户说明"基于可见图表提取，可能缺少盘中极端点"，不得用收盘价替代。

#### 1.4 数据读取输出

数据读取完成后，输出四要素，作为后续数浪的基准：

1. 数据时间范围（如 2026-01-01 至 2026-08-07）；
2. 区间最高/最低极端价位（含时间点）；
3. 当前价格位置（最新价相对区间高低点的位置）。
4. 复权方式 + 数据源 + 截至时间（强制披露；口径见 `references/data.md` 第 2 节）。

#### 1.5 数据自检（Data Self-Check）

进入数浪前，对 1.4 的数据基准执行数据自检，逐条勾选并输出《数据自检表》；**任一"不通过"必须退回重新取数或按 `references/data.md` 第 4 节降级链降级标注，禁止带病数据进入数浪**。

##### 自检条目（逐条勾选）

- [ ] OHLC 合理性：high ≥ low ≥ 0；open/close 均落在 [low, high] 内；volume ≥ 0；
- [ ] 日期升序、无重复；存在跳空缺口（停牌/节假日）时必须说明；
- [ ] 最新一根 K 线是否已收盘：盘中数据 vs 收盘数据必须注明；
- [ ] 极端价位（后续数浪与失效价位）一律取自 high/low 而非收盘价/开盘价（references/rules.md R20）；
- [ ] 数据范围至少覆盖一个完整 5+3 循环，不足时披露；
- [ ] 复权口径已标注（none/qfq/hfq）且全程一致；
- [ ] 数据源与截至时间已记录。

##### 《数据自检表》输出格式

| 检查项 | 判定结果（通过/不适用/不通过） | 说明 |
| --- | --- | --- |
| （自检条目） | 通过/不适用/不通过 | （判定依据与披露说明，如缺口区间、盘中时间、复权方式、历史长度等） |

- 任一"不通过"：退回重新取数或降级标注，修正后重新自检，**不得继续数浪**；
- 判定为"不适用"时须注明原因（如分钟线无复权概念）；
- 自检表随分析报告归档：并入 `assets/分析报告_report.md`【1. 分析前提】或附录，随报告交付。

### 2. 趋势方向判定

#### 2.1 趋势定义

趋势 = 在指定级别上，由同向推动浪主导、调整浪未改变方向的价格状态；**趋势必须绑定级别**，不声明级别的"趋势判断"无意义（references/terminology.md 1.7.7）。

#### 2.2 判定方法

**方法 A：从更大级别图表观察**

- 若更大级别处于推动浪阶段（1、3、5 浪中）：当前级别趋势与更大推动浪同向（references/terminology.md 1.1.2）。
- 若更大级别处于调整浪阶段（A-B-C 或联合形中）：当前级别趋势与调整浪反向——即更大一级推动浪的方向未变，当前级别的调整结束后仍将沿推动浪方向运行（references/terminology.md 1.1.3；方向身份见 references/rules.md R25）。

> 注意：调整阶段内部的短期运动仍随调整浪展开（A、C 与调整同向，B 与调整反向，references/rules.md R25）；"趋势与调整反向"指的是以更大一级推动浪为参照的长期方向。

**方法 B：从当前级别历史结构观察**

- 找出最近一个完整的 5+3 循环（推动 1-2-3-4-5 + 调整 A-B-C，references/terminology.md 1.1.6）。
- 若当前处于新一轮推动段：趋势方向 = 推动浪方向（上升/下降）。
- 若当前正处于调整段：短期方向 = 调整方向，长期方向 = 更大级别推动浪方向。

**方法 C：通道与均线辅助**

- 价格运行在上升通道且高点/低点不断抬高 → 上升趋势；反之 → 下降趋势。
- 通道与均线是辅助工具，只能用于定位与概率参考（references/guidelines.md G3；references/terminology.md 1.7.3），**不能替代波浪结构判定**。

#### 2.3 方向输出

必须明确给出三种结论之一：

- **上升趋势**；
- **下降趋势**；
- **方向不明**——此时必须说明原因（如结构不完整、处于大级别调整中段、数据不足）。

### 3. 分析级别选择（Degree Selection）

#### 3.1 强制要求

任何分析输出必须声明当前分析级别（Degree）；不声明级别的数浪视为无效输出（references/rules.md R22；references/terminology.md 1.2.1）。

#### 3.2 选择原则

- **原则 1（结构可识别）**：级别选择应使当前行情呈现"可识别的波浪结构"；若某级别图表上价格运动杂乱无章、无法划分摆动，则该级别不适合分析，应放大或缩小一个级别（references/terminology.md 1.2.3）。
- **原则 2（级别是相对的）**：级别名称只表示相对位置，不绑定固定时间；实战中可参考常见时间尺度（references/terminology.md 1.2.2）。
- **原则 3（自洽与变更）**：一旦声明级别，内部嵌套必须自洽；级别变更时必须说明理由（references/rules.md R4、R22）。

#### 3.3 级别输出格式

统一使用「级别中文名（英文名）+ 标注符号 + 当前所处浪段」：

- 示例："日线级别，中浪 Intermediate（(1)(2)(3)(4)(5)），当前运行第 (3) 浪内部。"
- 示例："小时级别，细浪 Minute（i ii iii iv v），当前处于 iii 浪延长中。"

### 4. 级别与趋势的绑定输出

#### 4.1 核心约定

趋势必须绑定级别（references/terminology.md 1.7.7）：同一段行情在日线级别是上升趋势、在周线级别可能只是调整浪的一部分，两者不矛盾。输出时必须同时给出"级别 + 趋势方向"，缺一即为不完整输出。

#### 4.2 输出模板（分析市场输出卡）

```
【分析市场输出卡】
- 分析级别：中浪 Intermediate（(1)(2)(3)(4)(5)），当前处于第 (3) 浪内部
- 趋势方向：上升趋势（依据：周线处于推动浪阶段；日线高点/低点不断抬高）
- 数据基准：2026-01-01 至 2026-08-07；区间最高 3,450（2026-06-15）、最低 2,800（2026-02-03）；当前价格 3,320；复权方式 qfq（前复权，hfq 不可得自动降级）、数据源 腾讯 fqkline、截至时间 2026-08-07 15:00
- 当前所处浪段：第 (3) 浪内部（尚未完成）
- 方向不明说明（如适用）：结构不完整，暂无法判定
```

输出卡完成后，交接给 `references/workflow.md（第 2 步）`。

### 5. 主要摆动识别与输出交接

#### 5.1 摆动标记

找出主要摆动（显著的高点/低点序列），作为数浪的原始输入（AGENTS.md 分析流程第三步）：

- 摆动点一律用**极端价位**标记（含影线），不用收盘价（references/rules.md R20）。
- 标记范围至少覆盖一个完整 5+3 循环（references/terminology.md 1.1.6），否则无法判断当前处于推动段还是调整段。
- 摆动的"显著"程度由当前分析级别决定：与声明级别匹配的摆动才进入序列，过小摆动剔除。默认筛选口径：摆动幅度 ≥ 当前级别最近完成浪长度的 0.382 倍，或时间跨度 ≥ 3 根当前周期 K 线（项目默认，须披露，见 references/defaults.md D11；references/terminology.md 1.7.11）。

#### 5.2 摆动序列输出

| 序号 | 类型（高/低） | 时间 | 极端价位 |
| --- | --- | --- | --- |
| 1 | 低 | 2026-02-03 | 2,800 |
| 2 | 高 | 2026-04-12 | 3,150 |
| 3 | 低 | 2026-05-20 | 2,950 |
| ... | ... | ... | ... |

#### 5.3 交接清单

分析市场步骤完成后，向 02 数浪移交四项内容：

1. 数据基准（时间范围 + 极端价位 + 当前价格位置 + 复权方式/数据源/截至时间）；
2. 级别声明（含标注符号与当前浪段）；
3. 趋势方向（上升/下降/方向不明 + 依据与原因）；
4. 主要摆动序列（极端价位标记）。

### 6. 常见错误与纠正

#### 错误 1：不声明级别就开始数浪

- **错误表现**：直接说"这是第 3 浪"，没有级别。
- **违反规则**：R22——不声明级别的数浪视为无效输出（references/rules.md R22）。
- **纠正方法**：先完成第 3 节级别选择与声明。
- **正确输出**："日线级别，中浪 Intermediate，当前处于第 (3) 浪内部。"

#### 错误 2：用收盘价确定极端价位与摆动点

- **错误表现**：用日线收盘价标记摆动高/低点。
- **违反规则**：R20——价格区域与长度判定按极端价位（含影线）（references/rules.md R20）。
- **纠正方法**：取最高价/最低价（含盘中极端点）标记摆动。
- **正确输出**：摆动点标为"2026-06-15 极端高点 3,450"而非"收盘 3,410"。

#### 错误 3：周期与分析级别不匹配

- **错误表现**：声明"周线级别 Cycle"却只用 1 分钟 K 线分析。
- **违反原则**：级别与时间尺度应匹配（references/terminology.md 1.2.2 常见时间尺度）。
- **纠正方法**：按声明级别选择对应周期数据；数据不足时缩小级别或向用户索取更长历史。
- **正确输出**："分析级别：循环浪 Cycle（周线级别数据）；已获取近 10 年周 K。"

#### 错误 4：方向不明却不说明原因

- **错误表现**：输出"方向不明"四个字，没有任何依据。
- **违反原则**：方向不明的输出必须给出原因（本文件 2.3 节约定）。
- **纠正方法**：说明是结构不完整、处于大级别调整中段，还是数据不足。
- **正确输出**："方向不明：周线处于第 4 浪调整中段，日线尚未走出可识别的 5+3 循环。"

#### 错误 5：把均线/通道当作结构判定

- **错误表现**：价格站上 20 日均线就宣布"上升趋势"。
- **违反原则**：通道与均线是辅助工具，不能替代波浪结构判定（references/guidelines.md G3；references/terminology.md 1.7.3）。
- **纠正方法**：先按方法 A/B 做结构判定，通道与均线只用于定位与概率参考。
- **正确输出**："上升趋势（依据：日线完成 5+3 循环后进入新一轮推动段）；20 日均线位于价格下方，与通道上轨共同作为辅助参考。"

---

**小结**：分析市场 = 数据准备（极端价位基准，R20）→ **数据自检（1.5，带病数据退回）** → 趋势方向（绑定级别，references/terminology.md 1.7.7）→ 级别声明（R22、R4）→ 摆动标记（极端价位）→ 输出"分析市场输出卡"交接给 02 数浪。本文件只做入口，不越权数浪。

## 2. 第 2 步 数浪（Count Waves）——工作流核心

> **本文件定位**：整个分析流程的第二步，接收 `references/workflow.md（第 1 步）` 的输出，执行"找出主要摆动 → 枚举候选数法（主 + 替代）→ 检查 Rule → 检查 Guideline → 标注失效价位 → 概率排序"。
>
> **前置依赖**：术语见 `references/terminology.md`，规则见 `references/rules.md`，概率经验见 `references/guidelines.md`，输入与交接见 `references/workflow.md（第 1 步）`，比率目标见 `references/fibonacci.md`。

### 1. 接收输入与确认前提

#### 1.1 唯一输入

本文件以 01 分析市场的"分析市场输出卡"为唯一输入，包括五项：分析级别、趋势方向、数据基准、当前所处浪段、主要摆动序列（见 01 分析市场 4.2、5.3）。

#### 1.2 输入确认 Checklist

数浪前必须确认四项输入齐全，任何一项缺失应退回第 1 步 补充，不得擅自假设（references/rules.md R22、R24）：

1. **分析级别声明**（references/rules.md R22）；
2. **趋势方向结论**（上升/下降/方向不明，含依据）；
3. **数据基准**（时间范围 + 最高/最低极端价位 + 当前价格位置）；
4. **主要摆动序列**（极端价位标记，至少覆盖一个完整 5+3 循环）。

#### 1.3 级别与趋势锁定

- 数浪过程中不得更改 01 声明的级别和趋势方向；只有触发 references/rules.md R19 失效后，才按流程回退到 01 重新分析（见 01 分析市场 4.1；references/rules.md R4）。
- 级别变更必须说明理由（references/rules.md R22）。

### 2. 主要摆动的波浪划分

#### 2.1 划分原则

先找推动浪（五浪结构），再找调整浪（三浪或其变体）；推动浪与父浪同向，调整浪与父浪反向（references/terminology.md 1.1.2、1.1.3；references/rules.md R1、R2）。

#### 2.2 标注规范

推动浪用 1-2-3-4-5，调整浪用 A-B-C，复合结构用 W-X-Y-Z，三角形用 A-B-C-D-E；同一张图不得混用两套符号（references/terminology.md 1.2.4；references/rules.md R23）。

#### 2.3 级别嵌套

当前级别的"第 3 浪"在更小级别上应能展开为完整的 1-2-3-4-5 或 A-B-C；若无法嵌套，需检视级别选择是否正确（references/terminology.md 1.2.3；references/rules.md R4）。

#### 2.4 操作步骤

1. 把摆动序列按"高/低交替"标记；
2. 尝试以"5+3"为单元组织摆动（references/terminology.md 1.1.6）；
3. 对每个候选浪段做子浪展开检查（更小级别是否可数）；
4. 无法展开的候选段标为"待确认"（候选段状态，非清单结论态；按清单三态应判"不适用（前提未证实）"），不得强行命名。

### 3. 枚举候选数法（主方案 + 替代方案）

#### 3.1 强制要求

任何分析必须同时给出主方案（Primary Count）和至少一个替代方案（Alternate Count），每个方案独立标注失效价位（references/rules.md R24；references/terminology.md 1.7.5）。

- **主方案**：概率最高的数法，通常基于最清晰的结构、最符合 Guideline 的形态、最常见的位置偏好。
- **替代方案**：行情存在歧义时给出的次优但合法数法；数量通常 1–2 个，极度模糊时可 3 个但需说明原因（references/terminology.md 1.7.5）。

#### 3.2 候选数法来源

歧义通常来自五类分歧：

1. 推动浪 vs 调整浪的身份分歧；
2. 延长浪 vs 正常五浪的分歧；
3. 失败浪 vs 仍在延伸的分歧；
4. 级别平移（当前级别的 1-2-3 是否只是更大级别第 1 浪）；
5. 复合结构（W-X-Y）vs 单一形态（A-B-C）的分歧。

候选数法之间出现形态边界归属分歧（如双锯齿形 vs 联合形、三角形 vs 楔形 vs 平台形）时，按 `references/rule_conflicts.md` 第 3 节裁决，不得凭偏好直接取舍。

#### 3.3 候选方案四要素

每个候选方案必须包含：方案名称、浪段标注、结构说明、关键比率目标（references/fibonacci.md 第 1 节速查表）。

### 4. 规则检查（Rule Check）

对每个候选方案逐条检查 06 的硬性规则，使用 06 规则速查索引，按组核对：

| 检查组 | 规则编号 |
| --- | --- |
| 结构身份 | R1、R2、R3、R4、R21、R25、R30 |
| 推动浪三铁律 | R5、R6、R7 |
| 楔形例外 | R8、R9、R10、R11 |
| 调整浪分类 | R12、R13、R14、R15、R16、R28 |
| 三角形 | R15、R27 |
| 计数规则 | R18 |
| 失败浪 | R17、R29 |

检查方法：逐条核对；任何一条不满足，该方案标记为"规则失效"，不得进入概率排序；若主方案失效，立即启用替代方案（references/rules.md R19）。

输出格式：

| 规则编号 | 检查结果（通过/不适用/违反） | 备注 |
| --- | --- | --- |
| R5 | 通过 | 第 2 浪终点 130 > 第 1 浪起点 100 |

### 5. 指导原则检查（Guideline Check）

对通过 Rule Check 的方案，逐条检查 07 的指导原则，使用 07 指导原则速查索引，按组核对：

| 检查组 | 原则编号 |
| --- | --- |
| 交替原则 | G1 |
| 等长原则 | G2 |
| 通道 | G3–G4 |
| 斐波那契比率 | G5–G8 |
| 延长/失败经验 | G9–G13、G22 |
| 形态频率与位置 | G14–G19、G23–G25 |

评分方法：每条 Guideline 命中 +1 分，未命中/不适用 0 分，明显违反 −1 分；得分越高，概率权重越高（references/guidelines.md G21）。

输出格式：

| 原则编号 | 判定（命中/未命中/违反） | 得分（+1/0/−1） | 说明 |
| --- | --- | --- |
| G1 | 命中 | 第 2 浪锯齿形（陡直），第 4 浪横向，符合交替 |

### 6. 失效价位标注与概率排序

#### 6.1 失效价位标注

- 每个通过 Rule Check 的方案必须标注独立的失效价位（references/rules.md R19、R20；references/terminology.md 1.7.1）。
- **规则失效价位**：由 R5、R6、R7、R17、R29 等推导，价格触及即方案失效（references/rules.md 第 6 节（规则失效价位））。
- **结构失效价位**：预期结构未形成时的降级价位（references/rules.md 第 6 节（规则失效价位）注；references/terminology.md 1.7.1）。
- 所有失效价位按极端价位（含影线）判定（references/rules.md R20）。

#### 6.2 概率排序

按 references/guidelines.md G21 的四个指标评分：Guideline 命中数、结构完整度、斐波那契共振、失效价位距离（越远越安全）。**统一评分口径**：加权总分 = 2×Guideline 得分 + 结构完整度 + 斐波那契共振 + 失效距离（权重 2:1:1:1），各项 0–2 分档与"失效距离相近"判定带见 references/workflow.md 第 5 步第 7 节（概率重新排序）；初始轮与更新轮使用同一口径（references/workflow.md 第 5 步第 7 节（概率重新排序））。排序不产生合法性判断，只影响仓位与计划（references/guidelines.md G21；references/terminology.md 1.7.6）。

输出格式：

| 方案名称 | Rule 通过 | Guideline 得分 | 共振区 | 失效价位距离 | 综合排序 |
| --- | --- | --- | --- | --- | --- |
| 主方案 | 是 | +3 | 有 | 远 | 1 |

#### 6.3 最终输出

主方案（排序第一）+ 替代方案（排序第二）+ 其余备选（如有）。

### 7. 工作流衔接与输出交接

本文件是 5 步工作流的第 2 步：分析市场（01）→ 数浪（本文）→ 验证数浪（03）→ 生成报告（04）→ 更新数浪（05）。

#### 7.1 向 03 验证数浪移交的内容

1. 主方案完整标注（浪段 + 结构 + 比率目标）；
2. 替代方案完整标注；
3. 各方案的 Rule Check 结果；
4. 各方案的 Guideline Check 结果；
5. 各方案的失效价位；
6. 概率排序表。

#### 7.2 向 04 生成报告移交的内容（预览）

主方案摘要 + 替代方案摘要 + 失效价位清单 + 交易计划建议（见 `assets/分析报告_report.md`）。

### 8. 实战示例

#### 示例 1：上升趋势，疑似第 4 浪横盘调整

**输入（01 输出卡摘要）**：日线级别 Intermediate；趋势方向：上升；摆动序列（极端价位）：

| 序号 | 类型 | 极端价位 | 初步划分 |
| --- | --- | --- | --- |
| 1 | 低 | 100 | 第 1 浪起点 |
| 2 | 高 | 160 | 第 1 浪终点 |
| 3 | 低 | 130 | 第 2 浪终点 |
| 4 | 高 | 220 | 第 3 浪终点 |
| 5 | 低 | 185 | 第 4 浪 A 段低点 |
| 6 | 高 | 245 | 第 4 浪 B 段高点 |
| 当前 | — | 235 | 第 4 浪 C/D 进行中 |

**候选数法**：

| 方案 | 浪段标注 | 结构说明 | 关键比率目标（references/fibonacci.md 第 1 节） |
| --- | --- | --- | --- |
| 主方案：第 4 浪平台形 | 1-2-3-4（A-B-C） | 第 4 浪横盘浅回调，A、B 已走出，C 将略破 A 低点（references/rules.md R14、R28） | C ≈ 1.0×A（等长）或 1.618×A |
| 替代：第 4 浪三角形 | 1-2-3-4（A-B-C-D-E） | 第 4 浪纠缠，A-B-C 已走出，等待 D-E（references/rules.md R15、R27） | 第 5 浪 ≈ 三角形最宽距离（references/guidelines.md G15） |
| 备选：更大级别第 1 浪 | 更大级别 1 浪 + 2 浪回调 | 整个 1-2-3 只是更大级别第 1 浪，当前横盘是更大级别第 2 浪（references/rules.md R4；references/terminology.md 1.2.3） | 更大级别第 3 浪目标按 1.618× 第 1 浪 |

**Rule Check（节选）**：

| 规则 | 主方案 | 替代方案 | 备选 |
| --- | --- | --- | --- |
| R5 | 通过（130 > 100） | 通过 | 通过 |
| R6 | 通过（第 3 浪最长） | 通过 | 通过 |
| R7 | 通过（185 > 160） | 通过 | 通过 |
| R14/R28 | 通过（A、B 三浪，C 未完成） | — | — |
| R15/R27 | — | 通过（待 D-E 确认） | — |
| R4 | 通过 | 通过 | 通过（嵌套自洽） |
| R24 | 通过（主 + 替代齐全） | 通过 | 通过 |

**Guideline Check（节选）**：

- 主方案：G1 交替命中（第 2 浪锯齿形陡直 → 第 4 浪横向）+1；G23 平台形多在第 4 浪 +1；G5 浅回撤 +1 → **3 分**。
- 替代方案：G1 命中 +1；G23 命中 +1；G15 未确认 0 → **2 分**。
- 备选：G20 级别相对性（允许平移）0；G23 不适用 0 → **0 分**。

**失效价位**：

- 主方案：平台形 C 浪跌破 A 浪低点 185 属于正常完成过程，不构成失效（references/rules.md R14、R28）；若跌破 185 后连续 2 根收盘未收回（references/workflow.md 第 5 步第 2 节（失效价位监控）默认口径）→ 平台形假设降级为锯齿形或更深调整（结构失效，references/rules.md 第 6 节（规则失效价位）注）。硬性失效：价格跌破第 1 浪高点 160 → 违反 R7，当前级别推动浪数法失效（references/rules.md R7、R19、R20）。
- 替代方案：跌破三角形下边界（按 references/workflow.md 第 5 步第 2 节（失效价位监控）默认口径：连续 2 根收盘未收回）后失效（结构失效，references/rules.md 第 6 节（规则失效价位）注）。
- 备选：跌破更大级别第 1 浪起点 100（references/rules.md R19）。

**概率排序**：

| 方案 | Rule 通过 | Guideline 得分 | 共振区 | 失效价位距离 | 综合排序 |
| --- | --- | --- | --- | --- | --- |
| 主方案 平台形 | 是 | +3 | 无 | 远（160） | 1 |
| 替代 三角形 | 是 | +2 | — | 远 | 2 |
| 备选 级别平移 | 是 | 0 | — | 很远（100） | 3 |

**最终输出**：主方案 = 第 4 浪平台形；替代 = 第 4 浪三角形；备选 = 更大级别第 1 浪。触发顺序：跌破 185 后连续 2 根收盘未收回 → 平台形降级（优先检视锯齿形，references/rules.md R12，或启用备选）；横盘继续走出 D-E → 三角形替代成立（references/rules.md R15）；跌破 160 → 违反 R7，当前级别推动浪假设失效，回退 01 或启用备选（references/rules.md R19、R22）。

#### 示例 2：下降趋势末端，第 5 浪疑似失败

**输入（01 输出卡摘要）**：小时级别 Minute；趋势方向：下降；摆动序列（极端价位）：

| 序号 | 类型 | 极端价位 | 初步划分 |
| --- | --- | --- | --- |
| 1 | 高 | 100 | 第 1 浪起点 |
| 2 | 低 | 80 | 第 1 浪终点 |
| 3 | 高 | 90 | 第 2 浪终点 |
| 4 | 低 | 55 | 第 3 浪终点 |
| 5 | 高 | 70 | 第 4 浪终点 |
| 6 | 低 | 60 | 第 5 浪终点（疑似失败） |
| 当前 | — | 62 | 反弹中 |

**候选数法**：

| 方案 | 浪段标注 | 结构说明 | 关键比率目标（references/fibonacci.md 第 1 节） |
| --- | --- | --- | --- |
| 主方案：第 5 浪失败反转 | 1-2-3-4-5（失败） | 第 5 浪内部完整五浪且终点 60 未破第 3 浪低点 55（references/rules.md R17、R29） | 反弹目标：第 4 浪高点 70 或 0.618 回撤（references/guidelines.md G6） |
| 替代：第 5 浪延长中 | 1-2-3-4-5（延长内部） | 60 只是延长浪内部一个低点，第 5 浪未完成（references/rules.md R26；references/terminology.md 1.3.2） | 延长目标 1.618×/2.618×（references/guidelines.md G6） |
| 备选：级别误判 | 更大级别第 3 浪内部 | 当前 1-5 只是更大级别第 3 浪的子浪（references/rules.md R4） | 更大级别目标按 references/fibonacci.md 第 1 节速查表 |

**Rule Check（节选）**：

| 规则 | 主方案 | 替代方案 | 备选 |
| --- | --- | --- | --- |
| R5 | 通过（90 < 100） | 通过 | 通过 |
| R6 | 通过（第 3 浪最长） | 通过 | 通过 |
| R7 | 通过（70 < 80） | 通过 | 通过 |
| R17/R29 | 通过（第 5 浪内部五浪已确认，60 > 55） | 不适用（前提未证实）（内部未完成，不得提前标失败，references/guidelines.md G12） | — |
| R26 | — | 通过（暂无违规） | — |
| R4 | 通过 | 通过 | 通过（嵌套自洽） |

**Guideline Check（节选）**：

- 主方案：G22 命中（失败常现趋势末端）+1；G12 命中（子浪完整后才判定）+1；G1 交替命中 +1 → **3 分**。
- 替代方案：G12 命中（没有提前标失败）+1；G9 未命中 0 → **1 分**。
- 备选：G20 级别相对性 0；其余不适用 → **0 分**。

**失效价位**：

- 主方案：第 3 浪终点 55——跌破则失败假设失效（references/rules.md R29、R19、R20）。
- 替代方案：第 4 浪高点 70——升破则"第 5 浪延长中"失效（references/rules.md R19）。
- 备选：更大级别第 1 浪起点 100——升破则级别误判假设失效（references/rules.md R19）。

**概率排序**：

| 方案 | Rule 通过 | Guideline 得分 | 共振区 | 失效价位距离 | 综合排序 |
| --- | --- | --- | --- | --- | --- |
| 主方案 失败反转 | 是 | +3 | — | 近（55） | 1 |
| 替代 第 5 浪延长 | 不适用（前提未证实） | +1 | — | 近（70） | 2 |
| 备选 级别误判 | 是 | 0 | — | 远（100） | 3 |

**最终输出**：主方案 = 第 5 浪失败、趋势反转；替代 = 第 5 浪延长中；备选 = 级别误判。触发顺序：跌破 55 → 主方案失效 → 启用替代，数第 5 浪内部延长结构并检查三铁律（references/rules.md R26）；升破 70 → 替代也失效 → 主方案的反转预期得到确认，或按备选回退 01 重新确认级别（references/rules.md R19、R22）。

---

**小结**：数浪 = 输入确认（01 输出卡）→ 摆动划分（极端价位）→ 候选枚举（主 + 替代 + 备选）→ Rule Check（不过即废）→ Guideline Check（评分加权）→ 失效价位标注 → 概率排序 → 交接 03。规则决定"能不能数"，Guideline 决定"哪个更可信"，失效价位决定"什么时候换"。

## 3. 第 3 步 验证数浪（Validate Count）——工作流校验关卡

> **本文件定位**：整个分析流程的第三步，是数浪结果的**最终校验关卡**。接收 `references/workflow.md（第 2 步）` 的移交内容，执行"结构自洽验证 → Rule 复核 → Guideline 复核 → 失效价位验证 → 输出完整性检查"。**本文件只验证，不重新数浪**；发现问题时退回第 2 步 修正，不得擅自更改数法。
>
> **前置依赖**：术语见 `references/terminology.md`，规则见 `references/rules.md`，概率经验见 `references/guidelines.md`，输入来自 `references/workflow.md（第 2 步）`，比率基准见 `references/fibonacci.md`。

### 1. 接收输入与确认前提

#### 1.1 唯一输入

本文件以数浪环节的移交内容为输入，共 6 项（02 第 7.1 节）：初始轮次来自 02 数浪；更新轮次中，05 更新数浪升级后的方案亦按同一格式提交验证（05 第 4.2 节）。

1. 主方案完整标注（浪段 + 结构 + 比率目标）；
2. 替代方案完整标注；
3. 各方案的 Rule Check 结果；
4. 各方案的 Guideline Check 结果；
5. 各方案的失效价位；
6. 概率排序表。

#### 1.2 输入确认 Checklist

6 项全部齐全方可进入验证；任何一项缺失，退回第 2 步 补充（references/rules.md R24）：

- [ ] 主方案完整标注；
- [ ] 替代方案完整标注；
- [ ] Rule Check 结果；
- [ ] Guideline Check 结果；
- [ ] 失效价位；
- [ ] 概率排序表。

#### 1.3 验证原则

本文件只做"检查员"，不做"数浪师"：发现矛盾时标记问题并退回，**不得现场修改数法**。允许在验证表中记录"建议关注"项，具体修改由 02 执行（02 第 7.1 节）。

### 2. 结构自洽验证

| 检查项 | 判定标准 | 结论 | 备注 |
| --- | --- | --- | --- |
| 级别嵌套自洽 | 当前级别推动浪在更小级别能否展开为完整 1-2-3-4-5 或 A-B-C；标注"第 3 浪"但无法展开 → "级别嵌套疑问" | | （references/terminology.md 1.2.3；references/rules.md R4） |
| 标注规范 | 同一张图是否混用两套符号（推动用数字、调整用字母、三角形 A-B-C-D-E、复合 W-X-Y-Z） | | （references/rules.md R23；references/terminology.md 1.2.4） |
| 方向身份一致 | 每个浪的作用/反作用身份是否与父浪方向匹配；B 浪、X 浪是否被误标为推动浪 | | （references/terminology.md 1.1.5；references/rules.md R25、R30） |
| 计数一致 | 九浪序列是否等效为五浪；延长浪内部是否未被拆分为两个同级推动浪 | | （references/rules.md R18；特别检查：02 是否误将延长浪拆成两个同级推动浪） |

### 3. Rule 复核（Rule Re-check）

#### 3.1 复核方法

- 对 02 标记为"通过"的规则做**抽样复核**；
- 对 02 标记为"不适用"的规则，检查"不适用"理由是否成立；
- 对 02 标记为"不适用（前提未证实）"的规则，检查前提是否确实未证实、跟踪与复核条件是否明确；前提已满足而仍判"不适用（前提未证实）" → 标记**"规则误判"**，退回第 2 步 重新检查；
- 复核若发现"通过"判定有误，标记为**"规则误判"**，退回第 2 步 重新检查（references/rules.md R19）。
- Rule 复核发现规则冲突（Rule vs Rule 表面冲突、Rule vs Guideline 冲突或形态边界归属分歧）时，按 `references/rule_conflicts.md` 第 2、3 节裁决，不得自行取舍条文。

#### 3.2 复核重点（按 06 规则速查索引分组）

| 复核组 | 规则编号 | 复核要点 |
| --- | --- | --- |
| 推动浪三铁律 | R5、R6、R7 | 极端价位判定是否正确（references/rules.md R20）；第 4 浪与第 1 浪重叠时是否已检视楔形例外（R8、R9、R10、R11） |
| 调整浪分类 | R12、R13、R14、R15、R16、R28 | 锯齿形 A/C 是否五浪；平台形 A/B 是否三浪；变体分类条件是否同时满足 |
| 三角形 | R15、R27 | 位置是否合法（第 4/B/X 浪）；子浪是否全部为三浪 |
| 楔形 | R8、R9、R10、R11 | 重叠是否享受合法例外；位置与结构条件是否同时满足 |
| 输出规范 | R22、R23、R24 | 级别是否声明；标注是否混用；主备方案是否齐全 |

#### 3.3 复核输出格式

| 规则编号 | 02 原判定 | 复核结果 | 备注 |
| --- | --- | --- | --- |
| R5 | 通过 | 通过 | 极端价位判定正确 |

### 4. Guideline 复核（Guideline Re-check）

#### 4.1 复核要点

- 02 的评分是否遗漏了明显命中或明显违反的原则？
- 同一 Guideline 在不同方案间的评分是否一致？例如 G1 交替原则：主方案"锯齿 + 平台"应得 +1，替代方案"锯齿 + 锯齿"是否漏给 −1（references/guidelines.md G1）？
- 罕见形态（三锯齿形、三重三浪、扩张三角形）的概率权重是否已下调（references/guidelines.md G14）？

#### 4.2 复核重点

| 原则编号 | 复核要点 |
| --- | --- |
| G1 | 第 2 浪与第 4 浪调整类型是否确实交替 |
| G5–G8 | 比率目标计算是否使用正确基准浪；是否按极端价位计算（references/rules.md R20；references/fibonacci.md 第 2 节） |
| G9 | 一个推动浪是否被错误地数出两个延长浪 |
| G15 | 三角形后的推进距离估算是否使用最宽处距离 |

#### 4.3 复核输出格式

| 原则编号 | 02 原评分 | 复核评分 | 调整理由 |
| --- | --- | --- | --- |
| G1 | +1 | +1 | 第 2 浪锯齿形、第 4 浪横向，交替成立 |

### 5. 失效价位验证

| 检查项 | 判定标准 |
| --- | --- |
| 独立性 | 每个方案是否都标注了独立的失效价位（references/rules.md R24；references/terminology.md 1.7.5） |
| 极端价位 | 失效价位是否按极端价位（含影线）判定（references/rules.md R20） |
| 可执行性 | 价格触及该价位时能否明确判断方案失效，而不是模糊区间 |
| 规则对应 | 对应关系是否清晰：跌破第 1 浪起点 ↔ R5；进入第 1 浪价格区域 ↔ R7；突破第 3 浪终点 ↔ R29 |
| 结构失效 | 是否标注结构失效价位（如"预期延长浪未形成"的降级价位，references/rules.md 第 6 节（规则失效价位）注） |

验证输出格式：

| 方案名称 | 失效价位 | 对应规则 | 判定标准 | 可执行性 |
| --- | --- | --- | --- | --- |
| 主方案（规则失效） | 160 | R7 | 跌破第 1 浪高点（极端价位） | 可执行 |
| 替代方案（结构失效） | 三角形下边界 | references/rules.md 第 6 节（规则失效价位）注（结构失效口径） | 突破下边界（极端价位）且不回抽（确认窗口按 references/workflow.md 第 5 步第 2 节（失效价位监控）默认口径：连续 2 根收盘未收回） | 可执行 |

### 6. 输出完整性检查

| 检查项 | 要求 | 结果 |
| --- | --- | --- |
| R22 级别声明 | 报告中明确声明分析级别（references/rules.md R22） | |
| R23 标注规范 | 未混用两套标注符号（references/rules.md R23） | |
| R24 主备方案 | 同时给出主方案与至少一个替代方案，各自独立标注失效价位（references/rules.md R24） | |
| 概率排序依据 | 明确列出 Guideline 命中数、结构完整度、共振、失效距离（references/guidelines.md G21） | |
| 交易计划建议 | 基于概率排序给出仓位或计划建议（references/guidelines.md G21；`assets/分析报告_report.md`） | |

### 7. 验证结论与输出交接

#### 7.1 验证通过

所有检查项通过，向 04 生成报告移交最终确认的内容：

1. 验证后的主方案（含修正后的标注）；
2. 验证后的替代方案（含修正后的标注）；
3. 验证后的概率排序表；
4. 失效价位清单（规则失效 + 结构失效）；
5. 复核中发现的问题摘要（即使通过，也列出"建议关注"项）；
6. 交易计划建议概要。

#### 7.2 验证不通过

- 标记不通过项，退回 `references/workflow.md（第 2 步）` 修正；
- 严重问题（级别嵌套不自洽、R5、R6、R7 误判等）退回 `references/workflow.md（第 1 步）` 重新确认级别（references/rules.md R4、R22）。

#### 7.3 与 04 生成报告的衔接

移交内容必须可直接填入 `assets/分析报告_report.md` 的对应字段（02 第 7.2 节）。

### 8. 实战示例：验证 02 提交的"第 4 浪平台形 vs 三角形"

**02 移交内容摘要（假设）**：

- 主方案：第 4 浪平台形（A-B-C），C 浪进行中；浪段 1-2-3-4；比率目标 C ≈ 1.0×A（references/fibonacci.md 第 1 节）。
- 替代方案：第 4 浪三角形（A-B-C-D-E），D/E 进行中。
- Rule Check：R5、R6、R7 通过；R14 通过；R28 通过；R15/R27 通过（D/E 未完成，待复核）。
- Guideline Check：主方案 +2（G1、G23）；替代方案 +2（G1、G23）。
- 失效价位：主方案 = 160（R7）；替代方案 = 三角形下边界。
- 概率排序：主方案 1、替代方案 2。

**第 1 步 结构自洽验证**：

| 检查项 | 结论 | 备注 |
| --- | --- | --- |
| 级别嵌套 | 通过 | 第 3 浪在更小级别可展开 |
| 标注规范 | 通过 | 数字/字母未混用 |
| 方向身份 | 通过 | B、X 未误标为推动浪 |
| 计数一致 | 不适用 | 无延长浪 |

**第 2 步 Rule 复核**：

| 规则 | 02 原判定 | 复核结果 | 备注 |
| --- | --- | --- | --- |
| R5、R6、R7 | 通过 | 通过 | 极端价位判定正确（185 > 160） |
| R14 | 通过 | 判定过早 | C 浪尚未完成，不能确认五浪结构，应标"不适用（前提未证实）" |
| R28 | 通过 | **规则误判** | B 浪终点 245 超过 A 浪起点 220（具扩张/奔走特征），但 C 浪未完成，无法确认 C 相对 A 终点的位置——平台形三变体分类条件未同时满足（references/rules.md R28） |
| R15/R27 | 通过 | 不适用（前提未证实） | D/E 未完成，子浪三浪结构待验证 |

**第 3 步 Guideline 复核**：

| 原则 | 02 原评分 | 复核评分 | 调整理由 |
| --- | --- | --- | --- |
| G1（主方案） | +1 | +1 | 第 2 浪锯齿形、第 4 浪平台形，交替成立 |
| G1（替代方案） | 漏评 | **+1** | 三角形亦为横向调整，与第 2 浪锯齿形交替成立（references/guidelines.md G1） |
| G5–G8 | +1 | 通过（附约束） | 02 按极端价位计算，通过；C ≈ 1.0×A 自 B 终点投射 = 210，高于 A 浪终点 185，不满足平台形 C 应跌破 A 终点的变体约束（references/rules.md R28）——扩张特征下应提高倍数（1.618×/2.618×A）并校验目标区不得越过父级规则失效价位（如第 1 浪高点 160，references/rules.md R7）；本示例有效目标区 165–180 |
| G14 | 不适用 | 不适用 | 无罕见形态 |

**第 4 步 失效价位验证**：

| 方案 | 失效价位 | 对应规则 | 判定标准 | 可执行性 |
| --- | --- | --- | --- | --- |
| 主方案 | 160 | R7 | 跌破第 1 浪高点（极端价位） | 可执行 |
| 主方案（结构） | **缺失** | references/rules.md 第 6 节（规则失效价位）注 | 应补：C 浪跌破 185 后连续 2 根收盘未收回（references/workflow.md 第 5 步第 2 节（失效价位监控）默认口径）→ 平台形降级；或 C 浪未走出五浪结构 | **不通过** |
| 替代方案 | 三角形下边界 | references/rules.md R19 | 突破下边界且不回抽 | 可执行，但未注明按极端价位判定与确认窗口（references/workflow.md 第 5 步第 2 节（失效价位监控）默认口径）→ 补注 references/rules.md R20、05 第 2.2 节 |

**第 5 步 输出完整性检查**：

| 检查项 | 结果 |
| --- | --- |
| R22 级别声明 | 通过 |
| R23 标注规范 | 通过 |
| R24 主备方案 | 通过（主 + 替代齐全，各自有失效价位） |
| 概率排序依据 | 通过 |
| 交易计划建议 | 缺失——02 未提供，可延至 04 生成报告阶段补齐，但需注明 |

**验证结论：不通过，退回第 2 步 修正**：

1. R28 变体分类条件未同时满足，不得判"通过"；
2. 主方案补结构失效价位（references/rules.md 第 6 节（规则失效价位）注）；
3. 替代方案补 G1 评分（+1）与 R20 判定注记；
4. C 浪比率目标基准按 references/fibonacci.md 第 2 节重新核对；
5. 交易计划建议缺失需注明（延至 04）。

级别嵌套与 R5、R6、R7 复核通过，**无需退回第 1 步**。

---

**小结**：验证数浪 = 输入齐全（6 项）→ 结构自洽 → Rule 抽样复核 → Guideline 评分复核 → 失效价位可执行性 → 输出完整性 → 通过移交 04 / 不通过退回第 2 步（严重问题回 01）。验证者的底线：**只指出问题，不替 02 数浪**。

## 4. 第 4 步 生成报告（Generate Report）——工作流输出

> **本文件定位**：整个分析流程的第四步，把 `references/workflow.md（第 3 步）` 验证通过的结果写成可直接交付的 8 章节报告。本文件只做"表述规范化"，不改变数法与概率排序；发现矛盾时退回第 3 步，不得擅自修改（03 第 7.1 节）。
>
> **前置依赖**：术语见 `references/terminology.md`，规则见 `references/rules.md`，概率经验见 `references/guidelines.md`，输入来自 `references/workflow.md（第 3 步）`，报告模板见 `assets/分析报告_report.md`。

### 0. 输出归档（先建目录，再生成）

- **开始生成报告前，先创建本次分析的专属归档文件夹**：位于**项目根目录** `分析输出/` 下（本仓库示例：`C:\games\Softwares\Codex\波浪skill\分析输出\`；技能独立运行时取当前工作区根目录），文件夹名 = `标的名称_代码_YYYYMMDD_HHMM`（本地时间 4 位），例如 `科创50_1B0688_20260809_1522`。Windows 文件夹名禁止 `\ / : * ? " < > |`，时间不得使用冒号（`15:22` 非法，应写 `1522`）。
- 该文件夹内**只放两个交付文档**（分析报告_*.md 与 网页报告_*.html）；浪型图 PNG 仅存于 `图表/` 子目录作内嵌素材；**不生成**原始行情 CSV、独立的失效追踪、交易计划、变更日志文件（内容分别并入报告第 5/6/8 章）。
- 报告与网页中的浪型图由 `scripts/embed_report_images.py` 自动 base64 内嵌（见 2.3 节）；回退模式下使用绝对路径引用该文件夹内的 PNG，禁止引用仓库其他位置的文件。

### 1. 接收输入与确认前提

#### 1.1 唯一输入

本文件以验证环节的移交内容为输入，共 6 项：

**初始轮次（来自 03 验证数浪，03 第 7.1 节）**：
1. 验证后的主方案（含修正后的标注）；
2. 验证后的替代方案（含修正后的标注）；
3. 验证后的概率排序表；
4. 失效价位清单（规则失效 + 结构失效）；
5. 复核中发现的问题摘要（含"建议关注"项）；
6. 交易计划建议概要。

**更新轮次（来自 05 更新数浪，05 第 8.1 节）**：同为 6 项——前 5 项为更新后的主方案、替代方案、概率排序表、失效价位清单、交易计划建议概要；第 6 项为变更日志（承担问题摘要职能）。

#### 1.2 确认原则

- 03 结论为"验证不通过"时，本文件不接收，退回第 3 步/02（03 第 7.2 节）；
- 03 结论为"验证通过"时，只做格式与表述规范化，不改数法、不改排序、不改失效价位；
- 报告中的任何数法内容必须能在 03 移交内容中找到对应项，否则退回第 3 步 补齐。

### 2. 报告结构总览（8 章节）

| 章节 | 内容 | 来源 |
| --- | --- | --- |
| 1. 分析前提 | 分析日期、归档路径、分析级别、趋势方向、数据基准、数据自检表 | references/workflow.md 第 1 步输出卡（01 分析市场 4.2）与数据自检表（01 分析市场 1.5）；归档路径见本文第 0 节 |
| 2. 主方案 | 浪段标注、结构说明、比率目标、失效价位、**主方案 K 线图** | 03 移交 1；图表见本文 2.1 节 |
| 3. 替代方案 | 浪段标注、结构说明、比率目标、失效价位、**替代方案 K 线图** | 03 移交 2；图表见本文 2.1 节 |
| 4. 概率排序与理由 | Guideline 得分、共振区、失效价位距离、综合排序 | 03 移交 3 |
| 5. 失效价位清单 | 规则失效 + 结构失效，含触发后动作 | 03 移交 4 |
| 6. 交易计划建议 | 仓位、入场、止损、目标 | 03 移交 6 |
| 7. 风险提示 | 建议关注项、歧义说明 | 03 移交 5 |
| 8. 更新与跟踪 | 下次检查的触发条件 | 05 更新数浪 |

#### 2.1 K 线图 + 浪型标注输出

在获取行情数据后、报告定稿前，用 Python 绘制"K 线图 + 浪型标注"：

- 执行脚本：技能 `scripts/ew_chart.py`（优先 matplotlib + mplfinance；mplfinance 不可用时自动回退 matplotlib 原生蜡烛图，不额外引入依赖）；
- **每个候选方案两张图——全景图 + 放大图**（图片数量 = 候选方案数 × 2）：
  - 全景图：覆盖完整数据范围（数据起点不得擅自截断，图注标注"起点至最新"），只标注大级别结构（1-2-3-4-5 / A-B-C / W-X-Y）与大级别失效价位，禁止标注子浪细节；
  - 放大图：用 `--last N` 截取尾部区间（默认最近 250 根，或按用户指定），标注子浪编号（①②③ 或 i-iii）、支撑/阻力、失效价位与当前价格，建议 `--font-scale 1.2–1.4` 保证子浪标注清晰；
  - 歧义极大出现第 3 个备选时可加画，但必须在报告【7. 风险提示】说明原因（图表数量为工作流输出约定，见 2.1 节）；
- **每张图只画一个方案**，禁止把主备两套数法叠在同一张图上（避免混用标注，references/rules.md R23）；
- K 线用开盘/最高/最低/收盘四价绘制（含影线，按极端价位口径，references/rules.md R20），x 轴为时间、y 轴为价格；
- 每张图保存为 PNG，命名规范：`主方案_全景.png`、`主方案_放大.png`、`替代方案_全景.png`、`替代方案_放大.png`（references/rules.md R24 要求至少主备两方案；若极端情况仅 1 个合法方案，防御性输出其全景 + 放大 2 张）；交付文档中的图片嵌入由 2.3 节自动完成；
- 图片保存后必须验证文件存在且能正常渲染（用本地图像查看工具确认），否则不得交付。

**每张图必须包含的标注**：

1. 图标题：分析级别（references/rules.md R22）+ 方案名 + 标的与周期，如"日线级别 · Intermediate · 主方案：第 4 浪平台形（Flat）"；
2. 浪段标注：推动浪 1-2-3-4-5、调整浪 A-B-C、三角形 A-B-C-D-E、复合结构 W-X-Y-Z（references/rules.md R23），同一张图不混用两套符号；
3. 每一段浪的形态类型备注（箭头 + 文字注释，标注在浪段终点附近）：推动浪（Impulse）/延长浪（Extension）/失败浪（Truncation）；楔形：前置三角形（Leading Diagonal）/终结三角形（Ending Diagonal）；锯齿形（Zigzag）/双锯齿形（Double Zigzag）/三锯齿形（Triple Zigzag）；平台形（Flat）：规则（Regular）/扩张（Expanded）/奔走（Running）；三角形（Triangle）：收缩（Contracting）/扩张（Expanding）/奔走（Running）；联合形（Combination）：双重三浪（Double Three）/三重三浪（Triple Three）；
4. 关键价位标注：极端价位（含影线高/低点）用小十字标记并标注价格（references/rules.md R20）；失效价位用红色虚线 + 价格标签并注明对应规则编号，如"跌破 160（第 1 浪高点，references/rules.md R7）即失效"（references/rules.md R19、R20）；当前价格用水平实线标注；如涉及比率目标，用浅色区间或水平虚线标注斐波那契目标区（08 第 X 节）；
5. 注释可读性：所有标注带引线、不得互相遮挡；图片右下角或图例注明方案名。

**图表自检清单**（报告定稿前逐项勾选）：

- [ ] 候选方案数 × 2 = 图片数量（每方案全景 + 放大各 1 张）；
- [ ] 每张图标题含分析级别（references/rules.md R22）；
- [ ] 每张图未混用标注符号（references/rules.md R23）；
- [ ] 失效价位按极端价位标注且注明对应规则编号（references/rules.md R20、R19）；
- [ ] 每一段浪都有形态类型备注，三角形注明收缩/扩张/奔走；
- [ ] 图片文件存在且已按 2.3 节内嵌（或回退绝对路径），报告内能正常渲染。
- [ ] 网页报告由模板生成：保留模板头部特征注释（"波浪理论网页报告模板"）、结构标记（如 `class="pano-img"`）齐全、无 `{{字段}}` 残留；**非模板生成判"不通过"**。
- [ ] 全部输出文件位于 `分析输出/<文件夹名>/` 内（一次分析一个文件夹，见第 0 节）。

#### 2.2 输出顺序与末尾简洁总结

报告交付按以下顺序组织，便于快速查看与决策：

1. **简明分析说明（3–5 段以内）**：标的、周期与声明的分析级别（references/rules.md R22）；主方案与替代方案各一句话（references/rules.md R24）；Rule 校验结论（说明哪条规则决定取舍，例如"第 4 子浪与第 1 子浪重叠，违反 R7，故不标普通推动浪，改用楔形假设"）；Guideline 概率排序依据（主备评分与失效距离，references/guidelines.md G21）。禁止长篇展开——细节进入 8 章节报告正文；
2. **浪型标注图**：按第 2.1 节绘制，放在总结之前（图片数量 = 候选方案数 × 2，工作流输出约定，见 2.1 节）；
3. **简洁总结（所有内容最后）**：严格按固定字段输出（模板见 `assets/分析报告_report.md` 末尾"简洁总结"节），每字段 1–3 句话、总长度 ≤15 行；总结只给结论、关键价位与仓位，禁止复述完整分析过程；概率表述用"大概率/倾向于/通常"，禁止"必然/一定/绝对"（references/guidelines.md 前言）。

#### 2.3 图片内嵌与自包含交付

**网页报告必须由模板生成**（核心流程，写死）：

- 复制 `assets/网页报告_web_report.html` 为网页交付文档 → 替换全部 `{{字段}}`（字段清单以模板为准，见 `assets/网页报告_web_report.html`）→ 图区 4 个图片占位符（`{{主方案全景图路径}}`、`{{主方案放大图路径}}`、`{{替代方案全景图路径}}`、`{{替代方案放大图路径}}`）由 `scripts/embed_report_images.py` 注入 base64 → 校验模板特征（头部特征注释、`class="pano-img"` 结构标记、无 `{{字段}}` 残留）；
- **禁止从零手写 HTML 结构**，禁止在非模板文件上填充；纯对话型 AI（无文件工具）需按模板字段清单与最小结构骨架等价重建，并在输出头部注明"按模板结构等价重建"（清单与骨架见 SKILL.md「两种使用环境」）。

- 报告与网页归档后，运行 `scripts/embed_report_images.py <归档文件夹>` 自动内嵌（归档文件夹 `分析输出/<文件夹名>/` 内只放两个交付文档 + `图表/` 素材，见第 0 节）：
  - **网页报告.html**：把 `图表/` 下 4 张 PNG（主方案/替代方案 × 全景/放大）转 base64，填入 `{{主方案全景图路径}}` 等 4 个 `<img>` 占位符，不再依赖外部相对路径；
  - **分析报告.md**：把 4 张 PNG 转 base64，写成 `![说明](data:image/png;base64,...)`；若用户查看器不支持 data URI，加 `--md-keep-absolute` 回退为绝对路径引用，保留 `图表/` 目录并在文末注明回退原因。
- 生成后自动校验，任一不通过则以退出码 1 结束、禁止交付缺图文件：
  - html 必须包含 `data:image/png;base64,`，数量等于 `<img class="pano-img">` 数量（标准模板为 4），且无 `{{...图路径}}` 占位符残留，并保留模板头部特征注释（"波浪理论网页报告模板"）与结构标记（`class="pano-img"`）；任一缺失判"不通过"；
  - md 必须包含 4 个图片标记（base64 data URI，或回退模式下存在的绝对路径）。
- 校验不通过时，从原始模板重新生成后再次运行脚本；禁止手工粘贴 base64 或手工拼 HTML。

### 3. 报告撰写规范

- **术语规范**：统一使用「中文（English）」格式，与 01 术语文件一致（引用 01 术语文件前言）。
- **标注规范**：推动浪用 1-2-3-4-5，调整浪用 A-B-C，三角形用 A-B-C-D-E，复合结构用 W-X-Y-Z；不得混用（references/rules.md R23；references/terminology.md 1.2.4）。
- **级别规范**：每次提及浪段时必须附带级别，例如"日线级别 Intermediate 浪（3）"而非单独的"第 3 浪"（references/terminology.md 1.2.1；references/rules.md R22）。
- **概率表述**：使用"概率较高""倾向于""通常"等 Guideline 词汇；禁止写"必然""一定""绝对"（引用 references/guidelines.md 前言）。
- **失效价位表述**：必须写明"价格触及 X 价位（按极端价位，含影线）时，Y 方案失效，切换至 Z 方案"（references/rules.md R19、R20）。

### 4. 交易计划与概率的绑定

- 交易计划必须基于概率排序，不能脱离排序单独给出（references/guidelines.md G21）。
- 仓位按主方案与替代方案的 Guideline 得分差（主 − 替代）分三档：

> 得分差指 Guideline **原始得分**差（命中 +1 / 未命中 0 / 违反 −1 累计，不乘 2）；加权总分（2:1:1:1）仅用于概率排序与升级裁决（references/workflow.md 第 5 步第 7 节（概率重新排序）），不用于仓位档位。

| 得分差 | 仓位建议 |
| --- | --- |
| ≥3 分 | 按主方案建仓，替代方案作为止损触发后的退路 |
| 2 分 | 轻仓试探，以主方案为主，但保留替代方案的退出路径 |
| ≤1 分 | 观望或轻仓试探，等待结构清晰 |

- 失效价位距离评估：失效价位越远，仓位可越重；失效价位很近时，即使主方案概率高，也应控制仓位（references/guidelines.md G21"失效价位距离"指标）。
- 止损位以失效价位为参考，按极端价位（含影线）设置（references/rules.md R20）。

### 5. 常见报告错误与纠正

#### 错误 1：报告中不声明分析级别

- **错误表现**：通篇只写"第 3 浪""第 5 浪"，没有级别。
- **违反规则**：R22——输出必须声明级别（references/rules.md R22）。
- **纠正方法**：在【1. 分析前提】中明确声明。
- **正确报告示例**："分析级别：日线级别，中浪 Intermediate（(1)(2)(3)(4)(5)），当前处于第 (3) 浪内部。"

#### 错误 2：只写主方案，省略替代方案

- **错误表现**：报告只有一套数法。
- **违反规则**：R24——必须同时给出主备双方案（references/rules.md R24）。
- **纠正方法**：【3. 替代方案】必须完整呈现，与主方案同规格。
- **正确报告示例**："主方案：第 4 浪平台形；替代方案：第 4 浪三角形；备选：更大级别第 1 浪。"

#### 错误 3：失效价位写得模糊

- **错误表现**：写"跌破 3,200 附近""180 左右"。
- **违反规则**：R20——失效价位按极端价位判定，必须精确（references/rules.md R20）。
- **纠正方法**：写明具体价位，例如"跌破 3,200（第 1 浪高点，极端价位）"。
- **正确报告示例**："价格触及 3,200（第 1 浪高点，极端价位）时，主方案失效，切换至替代方案。"

#### 错误 4：用"必然""一定"描述走势

- **错误表现**："第 5 浪一定不会失败""调整必然结束"。
- **违反原则**：references/guidelines.md 前言——Guideline 违反只降概率，不使数法失效；概率性规律不能升级为确定性预测。
- **纠正方法**：改用"大概率""倾向于""通常"。
- **正确报告示例**："终结三角形出现后，后续大概率出现快速反向运动（references/guidelines.md G10）。"

#### 错误 5：交易计划与概率排序脱节

- **错误表现**：主方案 Guideline 得分低、替代方案得分高，却建议按主方案重仓。
- **违反原则**：交易计划必须基于概率排序（references/guidelines.md G21）。
- **纠正方法**：按第 4 节"概率与仓位绑定"规则调整。
- **正确报告示例**："主方案 +2 分、替代方案 +0 分，得分差 2 分 → 轻仓试探，以主方案为主，保留替代方案的退出路径；不得重仓（references/guidelines.md G21）。"

### 6. 工作流衔接

- 本文件是 5 步工作流的第 4 步：分析市场（01）→ 数浪（02）→ 验证数浪（03）→ 生成报告（本文）→ 更新数浪（05）。
- 报告生成后，向用户交付完整报告；全部输出文件保留在 `分析输出/<标的_代码_YYYYMMDD_HHMM>/` 中，供 05 更新数浪时比对（05 更新数浪见 `references/workflow.md（第 5 步）`）。
- 报告中的失效价位清单是 05 的输入：05 在新数据到达后，检查这些失效价位是否被触发（03 第 5 节失效价位验证）。

### 7. 实战示例：生成完整 8 章节报告

**03 验证通过的移交内容（假设）**：主方案 = 第 4 浪平台形；替代方案 = 第 4 浪三角形；两者 Rule 均通过；Guideline 得分主 +3、替代 +3（复核补评 G1 后）；概率排序主 1、替代 2。

---

**【1. 分析前提】**

- 分析日期：2026-08-07
- 分析级别：日线级别，中浪 Intermediate（(1)(2)(3)(4)(5)），当前处于第 (4) 浪内部（第 4 浪形态待定：平台形 vs 三角形）（references/rules.md R22；references/terminology.md 1.2.1）
- 趋势方向：上升趋势（依据：周线处于推动浪阶段，日线高点/低点不断抬高）
- 数据基准：2026-01-01 至 2026-08-07；区间最高 245（2026-07-20）、最低 100（2026-01-05）；当前价格 235；复权方式 qfq（前复权，hfq 不可得自动降级）、数据源 腾讯 fqkline、截至时间 2026-08-07 15:00
- 数据自检表：全部通过（表并入本章末尾或附录，见 01 分析市场 1.5 节）

**【2. 主方案：第 4 浪平台形（Flat）】**

- 浪段标注：1（100→160）、2（160→130）、3（130→220）、4（A：220→185、B：185→245、C 进行中）
- 结构说明：第 4 浪以 3-3-5 平台形展开，A、B 为三浪，C 为五浪（references/rules.md R14、R28）
- 比率目标：C 浪目标按 references/fibonacci.md 第 1.3 节、第 2.2 节自 C 浪起点（B 终点 245）投射——1.0×A = 210、1.618×A ≈ 188.4、2.618×A ≈ 153.4；本例 B 已超过 A 起点（扩张特征），1.0×A/1.618×A 均高于 A 终点 185，不满足平台形 C 应跌破 A 终点的变体约束（references/rules.md R28），有效目标区 = 165–180（对应约 1.86–2.29×A），并满足双重约束：C 低于 185（平台形 C 破 A 终点）且高于 160（第 4 浪不得触发 R7）；等待 C 内部五浪确认（references/rules.md R14）
- 失效价位：价格触及 160（第 1 浪高点，极端价位）时，主方案失效，切换至替代方案（references/rules.md R7、R19、R20）；结构预警：C 浪跌破 185 后连续 2 根收盘未收回（references/workflow.md 第 5 步第 2 节（失效价位监控）默认口径）→ 平台形降级（references/rules.md 第 6 节（规则失效价位）注）

**【3. 替代方案：第 4 浪三角形（Triangle）】**

- 浪段标注：1-2-3-4（A：220→185、B：185→245、C/D/E 进行中）
- 结构说明：第 4 浪以 3-3-3-3-3 三角形展开，子浪 4/1 重叠为正常特征（references/rules.md R15、R27；references/terminology.md 1.4.8）
- 比率目标：第 5 浪 ≈ 三角形最宽处距离（references/guidelines.md G15）
- 失效价位：价格突破三角形下边界（极端价位）且不回抽（确认窗口按 references/workflow.md 第 5 步第 2 节（失效价位监控）默认口径：连续 2 根收盘未收回）时，替代方案失效，转回主方案或启用备选（references/rules.md R19、R20）

**【4. 概率排序与理由】**

| 方案 | Rule 通过 | Guideline 得分 | 共振区 | 失效价位距离 | 综合排序 |
| --- | --- | --- | --- | --- | --- |
| 主方案 平台形 | 是 | +3 | 无 | 远（160，距当前约 75 点） | 1 |
| 替代方案 三角形 | 是 | +3 | — | 较近（下边界约 200，距当前约 35 点） | 2 |

理由：两方案 Guideline 得分相同，按结构完整度与失效价位距离排序——主方案 A、B 已完成且结构清晰，替代方案 D/E 尚未完成、确认待定；失效价位距离上主方案 160 较远、替代方案下边界较近，结构安全边际亦支持主方案排序第一（references/guidelines.md G21）。

**【5. 失效价位清单】**

| 方案 | 类型 | 失效价位 | 判定标准 | 触发后动作 |
| --- | --- | --- | --- | --- |
| 主方案 | 规则失效 | 160 | 跌破第 1 浪高点（极端价位） | 切换至替代方案（references/rules.md R7、R19） |
| 主方案 | 结构失效 | 185 | 跌破 185（极端价位）后连续 2 根收盘未收回（references/workflow.md 第 5 步第 2 节（失效价位监控）默认口径）；或 C 内部复盘非五浪 | 平台形降级，检视锯齿形（references/rules.md 第 6 节（规则失效价位）注） |
| 替代方案 | 结构失效 | 185（三角形下边界基准：A 终点；C 终点确认前按 185 监控） | 突破且不回抽（极端价位；确认窗口按 references/workflow.md 第 5 步第 2 节（失效价位监控）默认口径） | 转回主方案或启用备选（references/rules.md 第 6 节（规则失效价位）注） |

**【6. 交易计划建议】**

- 主方案与替代方案 Guideline 得分差为 0（≤1 分）→ 按第 4 节规则：**观望或轻仓试探**，等待 C/D 结构展开后确认（references/guidelines.md G21）。
- 若必须操作：轻仓；止损参考主方案失效价位 160 上方（极端价位，references/rules.md R20）；目标参考平台形 C 浪完成后的第 5 浪（references/fibonacci.md 第 1.1 节）。
- 仓位说明：主方案失效价位 160 较远，结构安全边际略高于替代方案（替代方案三角形下边界约 200，失效位更近）；按第 4 节"失效价位越远仓位可越重"，允许轻仓；但因两方案 Guideline 得分接近，仍不重仓（references/guidelines.md G21）。

**【7. 风险提示】**

- 建议关注：C 浪比率目标按 references/fibonacci.md 第 2.2 节自 C 浪起点（B 终点）投射，且目标区须同时满足变体约束（C 低于 A 终点 185）与父级规则失效价位约束（C 高于第 1 浪高点 160，不触发 R7）——本示例有效目标区 165–180；替代方案 D/E 尚未完成，三角形确认待定（03 第 8 节）。
- 歧义说明：平台形与三角形均合法，最终区分依赖 C/D 子浪结构展开。

**【8. 更新与跟踪】**

- 触发条件：价格跌破 185 后连续 2 根收盘未收回 → 平台形降级；横盘走出 D-E → 三角形确认；跌破 160 → 主方案失效（references/rules.md R19）。
- 下一次更新：新数据到达后，由 05 更新数浪检查上述失效价位（`references/workflow.md（第 5 步）`）。

---

**小结**：生成报告 = 接收 03 移交（6 项）→ 按 8 章节组织 → 术语/标注/级别/概率/失效价位五条规范 → 交易计划与概率绑定 → 交付用户并归档 → 失效价位清单移交 05。报告是知识的最终输出，但**不产生任何新数法**。

## 5. 第 5 步 更新数浪（Update Count）——动态维护与生命周期管理

> **本文件定位**：整个分析流程的第五步，是数浪结果的动态维护与生命周期管理环节。接收 `references/workflow.md（第 4 步）` 的已确认输出，并在新市场数据到达后执行"失效价位（Invalidation Level）监控 → 触发条件判定 → 主备方案更新 → 调整浪（Corrective Wave）重评估 → 概率排序（Probability Ranking）重新排序 → 报告更新"。本文件只做"更新与维护"，不从头重新数浪；除非触发硬性重新数浪条件，否则遵循最小改动原则。
>
> **前置依赖**：术语见 `references/terminology.md`，规则见 `references/rules.md`，概率经验见 `references/guidelines.md`，基准输入来自 `references/workflow.md（第 2 步）`、`references/workflow.md（第 3 步）`、`references/workflow.md（第 4 步）`，调整浪形态细节见 `references/patterns.md（调整浪）`。

### 1. 接收输入与确认前提

#### 1.1 基准输入

本文件以 04 生成报告的移交内容为基准输入，共 7 项（6 项移交内容 + 分析级别声明）：

**6 项移交内容**（03 第 7.1 节；04 第 1 节）：
1. 已验证的主方案（Primary Count）完整标注（浪段 + 结构 + 比率目标）；
2. 已验证的替代方案（Alternate Count）完整标注；
3. 各方案的失效价位清单（规则失效 + 结构失效）；
4. 概率排序表；
5. 问题摘要（含"建议关注"项）；
6. 交易计划建议概要。

**第 7 项**：分析级别（Degree）声明（04 第 2 节报告第 1 章；references/rules.md R22）。

#### 1.2 新增输入

新到达的市场数据（K 线/价格行为），必须按极端价位（含影线）读取（references/rules.md R20）。

#### 1.3 输入确认 Checklist（7 项）

- [ ] 主方案完整标注；
- [ ] 替代方案完整标注；
- [ ] 失效价位清单；
- [ ] 概率排序表；
- [ ] 交易计划建议；
- [ ] 分析级别声明；
- [ ] 新数据（时间范围 + 极端价位 + 当前价格）。

任何一项缺失，退回对应前置步骤补充（references/rules.md R24）。

#### 1.4 更新原则

- **失效优先原则**：首先检查是否触及失效价位，触及则必须处理，不得忽略。
- **最小改动原则**：未触及失效价位时，优先对现有计数进行微调（如子浪细分修正），而非推翻整体结构。
- **级别不变原则**：除非当前级别结构被证明完全崩溃，否则不得擅自提升或降低分析级别（references/terminology.md 1.2.3；references/rules.md R4）。"完全崩溃"的判定标准即第 3.1 节硬性触发条件（H2 级别嵌套崩溃、H3 通道决定性突破等）；H2/H3 触发时本原则让位，允许重新确认级别，但必须说明理由（references/rules.md R22）。
- **本文件角色**："维护者"而非"数浪师"；发现问题时标记变更并说明理由，不得无依据地创造新计数。

### 2. 失效价位监控（失效触发判定）

#### 2.1 监控对象

主方案失效价位、各替代方案失效价位、结构失效价位（references/rules.md R24；03 第 5 节）。

**结构失效价位（Structural Invalidation Level）定义**：未违反硬性规则、但价格行为推翻了方案赖以成立的结构前提时，使该方案降级或作废的价格位（references/terminology.md 1.7.1；references/rules.md 第 6 节（规则失效价位）注）。常见前提示例：预期延长浪未形成、三角形边界被打破、平台形 C 浪未走出五浪。

#### 2.2 判定标准

- **规则失效价位**：按极端价位（含影线）判定，盘中触及即触发，无需收盘确认（references/rules.md R20）；不存在"有效突破"滤网。
- **结构失效价位**：按各形态文件定义的触发口径（如"突破边界且不回抽"）；本文件统一默认"不回抽"= 突破（按极端价位）后**连续 2 根 K 线收盘价位于边界外侧**（references/defaults.md D1），单根影线刺破仅记录、不触发。
- **口径优先级（结构失效价位）**：① 形态文件已定义的量化口径（`references/patterns.md（楔形）` 第 5 节、`references/patterns.md（三角形）` 第 5 节）＞ ② 本文件默认口径（连续 2 根收盘在外）＞ ③ 分析者自定义口径（必须在 04 报告中披露）。

#### 2.3 触及后的分支处理

| 情形 | 处理动作 |
| --- | --- |
| 主方案失效，替代方案未失效 | 主方案标记为"已失效"；首选替代方案升级为新主方案；升级后必须重新执行 03 验证数浪流程（03 第 2–6 节） |
| 主方案与所有替代方案均失效 | 触发硬性重新数浪，退回第 2 步 数浪环节；级别可能需要重新确认（references/rules.md R19） |
| 仅结构失效价位触及，规则失效价位未触及 | 当前方案降级为"观察中"；启动替代方案并行跟踪，但不立即升级（references/rules.md 第 6 节（规则失效价位）注） |

#### 2.4 未触及失效价位时

进入第 3 节"重新数浪触发条件判定"。

#### 2.5 输出

《失效监控记录表》：方案名称 | 失效价位 | 当前价格 | 是否触及 | 处理动作。

> **实例**：主方案失效价位 3,200（第 4 浪低点，极端价位）；新数据最低价 3,198、收盘 3,210。按 references/rules.md R20 已触及失效价位，主方案必须标记"已失效"并进入升级流程，不得以"收盘未破"为由忽略。

### 3. 重新数浪触发条件（硬性 vs 软性）

#### 3.1 硬性触发条件（必须放弃原有计数、退回第 2 步 重新数浪）

- **H1 推动浪三铁律违反**：价格行为违反 R5、R6、R7（references/rules.md R5、R6、R7）。
  > 实例：第 4 浪最低点跌破第 1 浪最高点且非楔形 → 主计数违反 R7，必须重数。
- **H2 级别嵌套崩溃**：当前级别的推动浪在更小级别上无法展开为任何合法结构（references/terminology.md 1.2.3；references/rules.md R4）。
  > 实例：日线"第 3 浪"在小时级别无法数出完整 1-2-3-4-5 或 A-B-C → 级别声明或数法必须重做。
- **H3 关键通道被决定性突破**：原分析依赖的趋势通道或修正通道被决定性突破，且突破后无法解释为子浪内部运动（references/guidelines.md G3、G4）。"决定性突破"量化口径与结构失效默认口径一致（05 第 2.2 节）：通道边界被突破（按极端价位）后连续 2 根 K 线收盘价位于通道边界外侧；仅影线刺破不触发。
  > **通道确认前置**：触发前必须确认通道的级别与端点——推动浪平行通道 = 连接第 1 浪终点与第 3 浪终点的基线 + 过第 2 浪终点（或第 4 浪终点）的平行线（references/guidelines.md G3；references/terminology.md 1.7.3）。第 3 浪延长时通道需重画（references/guidelines.md G4），不得用旧通道触发；楔形使用收敛/发散边界，不适用平行通道突破判定（`references/patterns.md（楔形）` 第 5 节）。
  > 实例：上升平行通道下轨被周线收盘价跌破，且随后 2 根周 K 收盘未收回 → 视为决定性突破。
- **H4 延长浪后的第五浪失败且无法归入其他形态**（references/rules.md R17、R29；02 第 3–4 节）。
  > 实例：第 5 浪延长内部走完九浪后，末端子浪未超越第 3 浪终点 → 触发更大级别重数。

#### 3.2 软性触发条件（建议重新评估，但不强制退回第 2 步）

- **S1 时间/比例关系严重偏离**：如第 3 浪长度与时间显著偏离第 1 浪的常见比率，且后续结构异常（references/guidelines.md G5–G8；references/fibonacci.md 第 4 节——比率未达本身不失效，系统性偏离才触发重估）。
  > 实例：第 3 浪长度不足第 1 浪的 0.618 倍且第 5 浪结构同样异常 → 优先视为级别误判，建议重估。
- **S2 调整浪细分持续不符**：调整浪细分持续不符合预期超过 3 个时间单位，且切换调整形态后 Guideline 评分显著提升（`references/patterns.md（调整浪）`；references/guidelines.md G21）。
  > 实例：平台形假设下 C 浪连续 3 个周期未走出五浪，按锯齿形重数后 G1/G23 命中数增加 → 建议切换。
- **S3 罕见复合结构早期迹象**：出现三锯齿形、三重三浪早期迹象，原计数概率权重需下调（references/guidelines.md G14）。
  > 实例：第二段锯齿后出现第二个 X 浪迹象 → 双锯齿形升级为三锯齿形的概率上升，原"单一锯齿形"权重下调。

#### 3.3 不触发条件（继续观察，不做更新）

- 正常子浪内部回撤或反弹；
- 结构失效价位：单根影线刺破边界但未满足第 2.2 节的"不回抽"确认口径（连续 2 根收盘在外）；
- 注：规则失效价位不存在"不触发"情形——按 references/rules.md R20，盘中触及极端价位即触发（references/rules.md R20）。

#### 3.4 输出

《触发条件判定表》：条件类型 | 具体表现 | 是否触发 | 处理动作。

### 4. 替代计数（Alternative Counts）的管理与升级

#### 4.1 替代方案的维护规则

- 任何时刻必须保持至少一个有效替代方案（references/rules.md R24）。
- 替代方案必须独立标注失效价位；规则强制共享的价位（如两方案同为第 4 浪数法时的 R7 价位）允许存在，但不得作为唯一价位——每个方案必须至少拥有一个仅自身触发、其他方案不触发的独立价位（03 第 5 节；`references/checklists.md（失效）` I5-2）。

#### 4.2 升级机制

主方案失效后，按以下裁决规则选择替代方案升级：

1. 先计算各替代方案的加权总分（第 7.2–7.3 节）；
2. 总分差 ≥2 分 → 高分者升级；
3. 总分差 ≤1 分 → 比失效距离（当前价格距失效价位更远者升级）；
4. 失效距离相近（两方案失效距离的相对差 < 当前价格的 10%，项目默认口径，定义见第 7.2 节）→ 比结构完整度（子浪细分更清晰者升级）；
5. 仍无法区分 → 两方案并列保留，下一数据周期再裁决。

升级后的替代方案必须重新经过 03 验证数浪流程（03 第 1–6 节；更新轮次的验证输入定义见 03 第 1.1 节）。

#### 4.3 降级机制

- 替代方案触及自身失效价位后，标记为"已失效"并从活跃方案中移除。
- 若活跃方案少于 2 个（主 + 替代），必须基于当前价格行为生成新的替代方案（02 第 3 节）。

#### 4.4 输出

《方案状态管理表》：方案名称 | 原状态 | 新状态 | 变更原因 | 失效价位。

> **实例**：主方案失效后，替代 A（第 5 浪延长）Guideline +3、失效距离 150 点；替代 B（第 5 浪失败）+2、失效距离 80 点 → 优先升级 A。

### 5. 调整浪的重新评估与形态切换

#### 5.1 触发条件

当前调整浪的细分与最初假设的调整形态持续不符时，启动重评估。

#### 5.2 形态切换决策树

- **假设锯齿形（Zigzag）**：若 A 浪为五浪但 C 浪未按预期完成五浪结构，且 B 浪回撤超过 A 浪的 61.8%，优先切换为平台形（Flat）（`references/patterns.md（调整浪）` 第 2–3 节；references/guidelines.md G5）。
  > 实例：锯齿形假设下 B 浪回撤达 A 浪的 75%，且 C 浪迟迟无五浪 → 改按平台形数。
- **假设平台形**：若 C 浪末端出现过度冲刺（超出常见比率目标仍无止境，references/fibonacci.md 第 4 节）或 B 浪终点明显超过 A 浪起点（扩张/奔走特征，references/terminology.md 1.4.7；references/rules.md R28），优先切换为扩张平台形；若 C 浪走出完整五浪且结构支持，按 references/rules.md R14 检查是否切换为锯齿形（`references/patterns.md（调整浪）` 第 3 节）。注：本文件不使用未经 references/rules.md、references/guidelines.md 定义的量化阈值（如"105%"）。
  > 实例：平台形 C 浪远超 2.618×A 仍无止境 → 按扩张平台形或更高级别调整重估。
- **假设三角形（Triangle）**：若 E 浪突破 A–C 连线或子浪出现五浪结构，立即取消三角形假设，切换为平台形或联合形（references/rules.md R15、R27；`references/patterns.md（三角形）` 第 6 节）。
> 实例：三角形 E 浪向下突破 A–C 连线后连续 2 根收盘未收回（references/workflow.md 第 5 步第 2 节（失效价位监控）默认口径）→ 三角形数法取消。
- **假设联合形（Combination）**：若 X 浪过深或第二个修正结构无法识别，考虑升级为更高级别的锯齿形或平台形（`references/patterns.md（调整浪）` 第 4 节）。
  > 实例：W-X 后第二个结构无法归类为已知修正形态 → 整体按更高级别锯齿形重估。

形态切换涉及边界归属歧义（如锯齿形 vs 平台形 vs 三角形 vs 联合形）时，按 `references/rule_conflicts.md` 第 3 节裁决顺序执行。

#### 5.3 切换后的验证

任何形态切换后，必须重新检查 R12、R13、R14、R15、R16、R28 分类条件是否同时满足（references/rules.md R12、R13、R14、R15、R16、R28）。

#### 5.4 输出

《调整浪形态切换记录》：原假设 | 不符证据 | 新假设 | 验证结果。

### 6. 延长浪（Extension）与失败第五浪（Truncated Fifth）的数浪更新

#### 6.1 延长浪后的重新定位

- **第三浪延长确认后**：第 5 浪通常与第 1 浪等长或呈 0.618 关系；若第 5 浪长度超过第 1 浪的 1.618 倍，需重新评估是否将第 5 浪识别为新的延长浪（references/guidelines.md G2、G9；references/fibonacci.md 第 1.1 节；02 第 3 节）。
  > 实例：第 3 浪延长确认后，第 5 浪走到第 1 浪的 1.618 倍仍无终止迹象 → 第 5 浪自身延长概率上升。
- **第五浪延长确认后**：后续调整浪（A 浪）的起点定位必须以延长浪的终点为基准，不得回溯；并预期双回撤结构（references/rules.md R18；references/guidelines.md G11）。
  > 实例：第 5 浪延长结束后，A 浪从延长浪终点起算，第一轮回撤后仍有第二轮回撤进入新高/新低。
- **九浪序列等效**：延长浪内部九浪必须等效为五浪，不得拆分为两个同级推动浪（references/rules.md R18；references/terminology.md 1.3.7）。
  > 实例：更新时发现"第 5 浪"被拆成两段同级推动浪 → 必须立即合并并按单浪重新标注。

**延长浪评估清单（确认"延长浪"成立时逐项核对）**：

- [ ] 第 5 浪（或疑似延长浪）内部可数出完整 1-2-3-4-5（references/rules.md R26）；
- [ ] 内部五浪满足 R5、R6、R7（references/rules.md R5、R6、R7）；
- [ ] 九浪序列按单浪等效，未拆分为两个同级推动浪（references/rules.md R18；references/terminology.md 1.3.7）；
- [ ] 延长浪与相邻非延长浪呈 1.618/2.618 倍关系（references/guidelines.md G6；references/fibonacci.md 第 1.4 节）；
- [ ] 第 5 浪延长后出现双回撤迹象（references/guidelines.md G11）；
- [ ] 级别嵌套自洽（references/rules.md R4）。

#### 6.2 失败第五浪（Truncated Fifth）的识别与后续

- **识别标准**：第 5 浪未能超越第 3 浪终点，且内部结构为完整的五浪（references/rules.md R17、R29；references/terminology.md 1.3.3）。子浪结构不完整时不得判定失败（references/guidelines.md G12）。
> 实例：下降趋势中，第 3 浪终点（低点）55；第 5 浪终点 60 高于第 3 浪终点（未创新低），且第 5 浪内部五浪完整 → 判定失败第五浪。
- **识别后必须立即重新评估更大级别结构**：失败第五浪通常预示更大级别的调整即将展开，或当前级别实际上为某个更大调整浪的 C 浪（references/guidelines.md G22；references/terminology.md 1.3.3）。
  > 实例：下降末端第 5 浪失败后，反弹走出三浪 A-B-C → 优先视为更大级别调整开始。
- **失败第五浪的失效价位**：必须重新计算，统一设定为**第 3 浪终点**（按极端价位）：上升趋势中，价格向上突破第 3 浪终点（高点）→ 失败假设失效；下降趋势中，价格向下跌破第 3 浪终点（低点）→ 失败假设失效（references/rules.md R29、R20）。
  > 实例：下降趋势中，价格重新跌破第 3 浪终点（低点）55 → 失败假设作废，切换"第 5 浪延长中"替代。

#### 6.3 输出

《特殊形态更新记录》：形态类型 | 原定位 | 新定位 | 后续影响。

### 7. 概率重新排序与报告更新

#### 7.1 重新评分触发

任何方案经历失效、升级、形态切换或延长浪确认后，必须重新进行 Guideline 评分（references/guidelines.md G21；02 第 5 节；03 第 4 节）。

#### 7.2 评分维度

| 维度 | 权重 | 计分口径 |
| --- | --- | --- |
| Guideline 命中数 | ×2 | 按 02 第 5 节统计（命中 +1、不适用 0、违反 −1），累计得分乘以 2 |
| 结构完整度 | ×1 | 子浪细分清晰且级别嵌套自洽 = 2；基本清晰 = 1；部分待确认 = 0 |
| 斐波那契共振 | ×1 | 关键比率（0.618、1.0、1.618、2.618，references/fibonacci.md 第 3 节）命中 ≥2 个 = 2；1 个 = 1；0 个 = 0 |
| 失效距离 | ×1 | 距当前价格 ≥ 1.618× 基准浪长度（当前级别最近完成推动浪）= 2；0.618×–1.618× = 1；< 0.618× = 0 |

> 注："失效距离相近"的判定带：两方案失效距离的相对差 < 当前价格的 10%（项目默认口径，见 references/defaults.md D7；可按当前波动率在 04 报告中调整并披露）；该口径仅用于第 4.2 节升级裁决的第 4 步。
>
> 注：本表权重（2:1:1:1）为项目默认口径，04 报告必须披露评分构成。

#### 7.3 排序规则

加权总分 = 2×Guideline 得分 + 结构完整度 + 斐波那契共振 + 失效距离；按总分降序排列；总分相同则优先选择失效距离更远的方案（references/guidelines.md G21）。

#### 7.4 交易计划调整

基于新的概率排序，按 04 第 4 节的三档规则给出仓位调整或计划修正建议。三档规则以主 − 替代的 Guideline **原始得分差**为准（命中 +1 / 未命中 0 / 违反 −1 累计，不乘 2）；加权总分用于排序与升级裁决（第 7.2–7.3 节），不直接作为仓位档位。

#### 7.5 输出

《更新后概率排序表》：方案名称 | Guideline 评分 | 结构完整度 | 共振数 | 失效距离 | 总分 | 排序。

> **实例**：原主方案移除后，替代 A（延长）总分 8、失效距离 150 点，替代 B（失败）总分 7、失效距离 80 点 → A 排序第一。

### 8. 输出交接与变更日志

#### 8.1 验证通过后的移交内容（向 04 生成报告反馈更新）

共 6 项，与 04 第 1.1 节"更新轮次"输入清单一致：

1. 更新后的主方案（含修正后的标注与级别声明）；
2. 更新后的替代方案（含修正后的标注与失效价位）；
3. 更新后的概率排序表；
4. 新的失效价位清单（规则失效 + 结构失效）；
5. 更新后的交易计划建议概要；
6. **变更日志（Change Log）**（承担原"问题摘要"职能，含建议关注项）。

#### 8.2 变更日志（Change Log）

记录本次更新中每一项改动的具体内容、改动原因、引用依据。示例："主方案因触及失效价位 3,200 降级，替代方案 Y 升级，引用 references/rules.md R24；新主方案已重新通过 03 验证，引用 03 第 7 节。"

#### 8.3 验证不通过

若更新过程中发现无法解决的矛盾（如级别嵌套崩溃、R5、R6、R7 硬性违反），标记为"需退回第 2 步 重新数浪"，并附问题摘要（03 第 7.2 节；references/rules.md R19）。

#### 8.4 与 04 生成报告的衔接

移交内容必须可直接覆盖 `assets/分析报告_report.md` 的对应字段，字段映射以 04 第 2 节 8 章节结构为准：

| 05 移交项 | 04 报告章节/字段 |
| --- | --- |
| 更新后的主方案（含级别声明） | 【1. 分析前提】（级别声明字段）、【2. 主方案】 |
| 更新后的替代方案 | 【3. 替代方案】 |
| 更新后的概率排序表 | 【4. 概率排序与理由】 |
| 新的失效价位清单 | 【5. 失效价位清单】 |
| 更新后的交易计划建议概要 | 【6. 交易计划建议】 |
| 变更日志（含问题摘要） | 【7. 风险提示】+【8. 更新与跟踪】 |

#### 8.5 输出归档规则

- 更新轮次的输出（更新后的报告、图表、变更日志等）一律写回同一标的的归档文件夹 `分析输出/<标的名称_代码_YYYYMMDD_HHMM>/`，不另建新文件夹。
- 变更日志追加到该文件夹内分析报告.md 的【8. 更新与跟踪】节（**不单独生成变更日志文件**），确保同一标的的完整演变可在同一文件夹内追溯。
- 确需从头重开（如硬性重新数浪后重构报告）时，新建带新时间戳的文件夹，并在旧文件夹分析报告.md 的【8. 更新与跟踪】节登记"已移交至新文件夹 <新文件夹名>"。

### 9. 实战示例：主方案失效后的完整更新流程

**示例背景（04 已确认输出）**：

- 主方案：第 5 浪常规推动，目标位 1.618× 第 1 浪（references/fibonacci.md 第 1.1 节）；
- 替代方案 A：第 5 浪延长（references/rules.md R26；references/terminology.md 1.3.2）；
- 替代方案 B：第 5 浪失败（references/rules.md R17、R29）；
- 主方案失效价位：第 4 浪低点（结构失效：跌破后连续 2 根收盘未收回，references/workflow.md 第 5 步第 2 节（失效价位监控）默认口径）。

**新数据**：价格接近主方案目标位后出现长上影线，随后回落并跌破第 4 浪低点（进入结构失效确认窗口），但未触及替代方案 A/B 的失效价位。

**更新流程**：

1. **失效价位监控**：按结构失效默认口径判定（05 第 2.2 节）——主方案跌破第 4 浪低点后连续 2 根收盘未收回 → 标记"已失效"（第 2.3 节）；若仅第 1 根收盘在外，标记"待确认（1/2）"继续跟踪。
2. **触发条件判定**：属于"主方案失效，替代方案未失效"，不触发硬性重新数浪（第 2.3 节分支 1；references/rules.md R19）。
3. **替代方案升级**：比较替代 A 与替代 B——替代 A Guideline 评分更高且失效距离更远 → 替代 A 升级为新主方案（第 4.2 节；references/guidelines.md G21）；升级后按 03 第 2–6 节重新验证。
4. **调整浪重评估**：当前位置若为第 5 浪延长，按第 6.1 节"延长浪评估清单"逐项核对——第 5 浪内部可数出完整 1-2-3-4-5、满足 R5、R6、R7、九浪等效单浪、比率呈 1.618/2.618、双回撤迹象、级别嵌套自洽（references/rules.md R26、R5、R6、R7、R18、R4；references/guidelines.md G6、G11；references/fibonacci.md 第 1.4 节）。
5. **延长浪更新**：确认延长浪后，后续 A 浪起点以延长浪终点为基准，不得回溯；更新通道（references/rules.md R18；references/guidelines.md G11、G3）。
6. **概率重新排序**：新主方案（原替代 A）重新评分；原主方案移除；替代 B 保留；若活跃方案不足 2 个，基于当前价格行为补充新替代（02 第 3 节；references/rules.md R24）。
7. **输出交接**：生成《变更日志》（含每项改动原因与引用依据），向 04 移交更新后的主/替代方案、概率排序表、失效价位清单与交易计划建议概要（第 8.1–8.2 节）。

---

**小结**：更新数浪 = 失效优先（第 2 节）→ 触发判定（第 3 节）→ 主备维护（第 4 节）→ 调整浪重估（第 5 节）→ 延长/失败特殊处理（第 6 节）→ 概率重排（第 7 节）→ 变更日志与交接（第 8 节）。底线两条：**未触发硬性条件不得推倒重数；任何变更必须留痕并给出引用依据**。
